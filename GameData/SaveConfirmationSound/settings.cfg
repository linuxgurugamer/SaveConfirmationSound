SaveConfirmationSoundSettings
{
	Debug = false
	SoundLocation = SaveConfirmationSound/click
	MinimumTimeBetweenSounds = 100 //if KSP saves more than once below this threshold (in milliseconds) the audio will not play again. used to prevent double audio plays
}